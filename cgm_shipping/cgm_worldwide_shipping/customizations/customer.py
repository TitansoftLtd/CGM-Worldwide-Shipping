"""Customer customizations (reserved).

Shipment type and mode of transport are intentionally **not** copied from Lead to Customer,
because one customer can run many shipments under different modes/types.
"""
